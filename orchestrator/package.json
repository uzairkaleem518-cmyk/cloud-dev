{
  "name": "cloud-dev-env-orchestrator",
  "version": "1.0.0",
  "description": "Small privileged daemon that is the ONLY process on a host allowed to talk to docker.sock. The main backend never touches Docker directly - it calls this daemon over HTTP/WebSocket, authenticated with a shared secret. Runs one instance per Docker host, so multiple instances = multiple hosts (see backend/services/hostRegistry.js).",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "seed:images": "node scripts/pullImages.js"
  },
  "dependencies": {
    "dockerode": "^4.0.2",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^7.4.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  }
}
